# Google Sheets API 有効化ガイド

`Google Sheets API has not been used in project 126305308570 before or it is disabled`エラーを解決するためのガイドです。

---

## 🔍 問題の原因

Google Cloud Console で、Google Sheets API が有効化されていない場合に発生します。

---

## ✅ 解決方法

### 1. Google Cloud Console にアクセス

以下のリンクから直接アクセスできます：

**直接リンク**: https://console.developers.google.com/apis/api/sheets.googleapis.com/overview?project=126305308570

または、以下の手順でアクセス：

1. [Google Cloud Console](https://console.cloud.google.com/)にアクセス
2. プロジェクト ID `126305308570` を選択
   - プロジェクトが表示されない場合は、プロジェクト名で検索してください

### 2. Google Sheets API を有効化

1. 上記のリンクにアクセス
2. **有効にする**ボタンをクリック
3. API が有効化されるまで数秒待機

### 3. 確認

API が有効化されると、以下のように表示されます：

- **API が有効です**というメッセージが表示される
- **無効にする**ボタンが表示される（有効化されている証拠）

---

## ⚠️ 注意事項

### API が有効化されてもエラーが続く場合

API を有効化した後、数分待ってから再試行してください。API の有効化がシステム全体に反映されるまで時間がかかる場合があります。

### プロジェクト ID が異なる場合

ログに表示されているプロジェクト ID（`126305308570`）が、実際に使用しているプロジェクトと異なる場合は、正しいプロジェクトで Google Sheets API を有効化してください。

---

## 🔍 確認方法

デプロイ後、Vercel Dashboard > Deployments > 最新のデプロイメント > Logs で以下を確認：

**正常な場合:**

```
[Sheets] ✅ Successfully saved to sheet 1/1
[handleMessage] ✅ Profile saved to sheet successfully
```

**エラーが発生している場合:**

```
[Sheets] ❌ Failed to save to sheet 1/1: Google Sheets API has not been used...
```

---

## 📚 関連ドキュメント

- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - その他のトラブルシューティング
- [VERCEL_ENV_VARIABLES.md](./VERCEL_ENV_VARIABLES.md) - 環境変数の設定方法
